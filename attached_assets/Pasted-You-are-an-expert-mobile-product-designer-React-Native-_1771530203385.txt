You are an expert mobile product designer + React Native developer specializing in self-hosted AI companion apps. Current date: February 19, 2026.

The app is "ClawCockpit" — a beautiful, secure, native-feeling iOS/Android companion for self-hosted OpenClaw (openclaw.ai, GitHub: openclaw/openclaw). It connects via WebSocket to the user's Gateway (:18789, Bonjour discovery or manual URL/Tailscale), replacing plain chat apps with rich visualizations.

Attached is the current dashboard mockup (first version). It's a good start but feels incomplete: missing connection/pairing UX, proactive alerts, deeper OpenClaw data ties (memory teasers, cron events, skill status), personality (lobster theme), and polish.

Your task: Evolve this into v2 designs + full spec.

1. Research refresh (use latest knowledge): Summarize current OpenClaw Gateway WS protocol, pairing (challenge/nonce/Bonjour/DM), session/memory/cron/events exposure, official node limitations, common user pain points from Discord/Reddit/GitHub issues.

2. Critique the attached mockup: List 10+ specific improvements (UX, visuals, data, accessibility).

3. Design system & branding: Define color palette (dark lobster: navy/black base, orange/red accents, claw gradients), typography, icons (custom claw variants?), animations (subtle on connect/task complete).

4. Produce detailed screen flows (text-based wireframes + descriptions):
   - Onboarding / First Launch (welcome → connect → pair → permissions → quick tour)
   - Connection Manager modal/sheet (auto-discovery + manual + status)
   - Dashboard v2 (enhanced: proactive banner, better metrics, memory teaser, customizable quick actions carousel/grid)
   - Chat screen (markdown rendering, file previews, voice input button, streaming indicator)
   - Tasks/Kanban screen (draggable boards synced from memory/cron, command feedback)
   - Memory & History browser (searchable timeline, filters by date/skill)
   - Quick action examples expanded (5–8 tiles with full labels/icons)

5. Tech notes for React Native/Expo implementation: Key components/libs for each screen (e.g., react-native-draggable-flatlist for Kanban, expo-notifications for proactive pushes, ws for Gateway).

6. Output format:
   - Executive Summary
   - Research Snapshot
   - Mockup Critique & Improvements
   - Design System
   - Detailed Screen Specs (use markdown tables + ASCII art wireframes where helpful)
   - Next Development Priorities (MVP screens to build first in Replit)

Be visual, practical, and fun — embrace the lobster meme energy where it fits. Make ClawCockpit feel like the premium, must-have face for every OpenClaw user.