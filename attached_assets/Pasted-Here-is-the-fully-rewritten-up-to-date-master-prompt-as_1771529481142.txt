Here is the fully rewritten, up-to-date master prompt (as of February 19, 2026).
Copy the entire block below and paste it into Grok, Claude, GPT-4o, or any top model. It now incorporates everything we discussed: the original vision + all the extra features (proactive notifications, memory browser, quick actions, skills marketplace, file viewer, multi-agent, voice, widgets, CRM timelines, offline queue, dashboards, biometric lock, lobster branding, etc.). It also forces deep real-time research into the current OpenClaw architecture (Gateway WS on :18789, pairing, official iOS/Android nodes, Canvas, skills format, memory as Markdown, heartbeat scheduler, etc.).

You are an expert full-stack mobile developer, AI agent integration specialist, product strategist, and App Store launch consultant with deep experience in self-hosted tools, secure local-first apps, and React Native/Expo projects.
User vision (research and expand this into a complete, production-ready product):
The user wants a beautiful native iOS + Android app (React Native + Expo) called something like “LobsterDesk”, “ClawVista”, “OpenClaw Hub”, or “ClawCockpit” that becomes the dedicated, secure, visual “mission-control” companion for a self-hosted OpenClaw instance — replacing plain Telegram/Signal/WhatsApp chat.
Instead of scattered text messages, the app delivers a rich, glanceable, interactive productivity cockpit: real-time chat + Kanban boards + calendar + CRM + proactive alerts + memory browser + skills marketplace + file viewer + quick actions + home-screen widgets and everything else that makes sense once you fully research OpenClaw in February 2026.
Core requirements:

100% private — connects directly to the user’s own OpenClaw Gateway (no central servers, no data leaves their network unless they choose).
Works locally (Bonjour/mDNS auto-discovery) and remotely (manual URL, Tailscale, Cloudflare Tunnel).
Built to be coded entirely in Replit today (Expo + cloud builds).
Launchable on Apple App Store and Google Play.

Your mission – execute in this exact order and deliver a complete structured report:

Deep Research Phase (mandatory – use your full knowledge as of February 19, 2026)
Official site (openclaw.ai), GitHub (github.com/openclaw/openclaw), docs (docs.openclaw.ai), blog, and latest community resources.
Detail the Gateway architecture (single Node.js process on :18789, WebSocket control plane/protocol), handshake (connect.challenge, nonce, pairing/Bonjour/DM approval, allowlists), node roles/scopes, real-time events, tool invocation, memory (Markdown on disk), cron/heartbeat scheduler, skills format, Canvas, voice/sensor nodes, official iOS/Android companion limitations, WebChat UI, and exactly how a third-party client authenticates, subscribes to sessions, sends/receives messages, reads/writes memory, pulls tasks/calendar/cron, renders proactive events, etc.
Security best practices, remote-access options, and App Store review considerations for self-hosted apps.

Market & Competitor Analysis
Compare official OpenClaw mobile nodes vs. what a rich dedicated app can offer.
Similar third-party clients (custom Claude/Anthropic apps, local AI dashboards, Notion mobile agents, etc.).
Unique value proposition: “The beautiful, secure cockpit your OpenClaw always deserved.”

App Concept & Branding
3 name + tagline + icon concept options (lobster-themed, dark mode first).
Overall UI/UX vision (bottom nav or tabs: Home/Dashboard | Chat | Kanban | Calendar | Memory | Skills | More).
Fun “lobster” personality touches (animations, reactions, dark lobster theme, Claw Mode).

Full Feature Specification
Group into MVP (v1.0), Phase 2 (v1.1–v1.2), and Stretch (v2.0+).
Include every feature below plus anything else high-value you discover during research:MVP – must ship
Gorgeous real-time chat (markdown, image/PDF preview, file upload, voice dictation, streaming).
Live Kanban boards (To Do / In Progress / Done / Deferred / Archived) synced from OpenClaw memory/cron/skills — drag & drop that sends natural-language commands back.
Full native calendar view + create/edit (pulls from any calendar skills).
Proactive push notifications & alerts (cron reminders, inbox zero, flight check-in, agent heartbeats, etc.).
Quick Actions / Command Palette (one-tap or widget: “Summarize inbox”, “Schedule meeting”, “Check GitHub”, etc.).
Searchable Memory & History Browser (past conversations, completed tasks, notes, timeline filters).
File & attachment viewer (preview images/PDFs/docs sent by agent).
Skills Marketplace (browse, one-tap install/uninstall popular community skills).
Connection manager (auto-discovery + manual URL + Tailscale support).
Biometric/PIN lock.
Phase 2
CRM / Relationship timelines (per-contact history if email/calendar skills active).
Dashboard widgets & iOS/Android home-screen integration (today’s agenda, pending tasks, agent status).
Offline queue + sync (queue commands when disconnected).
Voice TTS (read responses aloud).
Multi-agent / workspace switcher (personal vs work instances).
Task deep views (progress charts, dependencies if Linear/Monday skills active).
Agent health & logs monitor.
Stretch / Future
Custom no-code dashboards.
Gantt/timeline views.
Multi-device shared sessions.
Premium themes / advanced widgets via IAP.

Technical Implementation Plan (Replit-optimized)
Stack: React Native + Expo (EAS cloud builds — no Mac needed for iOS).
WebSocket client (exact auth/handshake flow matching official protocol).
State management, real-time updates, offline handling.
UI libraries: react-native-calendars, react-native-draggable-flatlist (Kanban), TanStack Query, Zustand, expo-notifications, expo-speech, expo-file-system, etc.
Security & background (keep-alive, biometric, sandboxing).
Step-by-step Replit development workflow + test with Expo Go + final EAS build commands.

App Store Launch Plan
Exact submission steps for Apple + Google Play.
Review-friendly description (emphasize self-hosted, user-owned Gateway, no data collection).
Privacy policy template points, required screenshots, keywords, screenshots strategy.
Pricing (free core + optional premium IAP).
Marketing: OpenClaw Discord/Reddit/GitHub, launch post template, “the app every OpenClaw user has been waiting for”.

Risks, Challenges & Mitigations + Timeline
App review risks, WebSocket keep-alive, battery, security disclosures.
Cost estimates (Expo, Apple $99/year, etc.).
Realistic solo-developer timeline using Replit.


Final Output Format (clean markdown, tables, code snippets where useful):

Executive Summary
Research Findings (current OpenClaw state)
App Concept & Branding
Detailed Feature Roadmap (MVP + Phase 2 + Stretch)
Technical & Replit Development Blueprint
Launch & Marketing Plan
Risks, Costs & Timeline
Ready-to-code Starter Code Structure (key files/components)

Be extremely thorough, practical, optimistic yet realistic. Use tables for feature comparison. This must be the complete blueprint the user can start coding from tomorrow in Replit.